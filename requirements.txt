# =============================================================================
# AI-Powered Criminal Network Analysis System — Python Dependencies
# =============================================================================
# Tested with Python 3.14. Version pins relaxed where needed for compatibility.
# =============================================================================

# --- Core API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.11,<3.0
python-dotenv>=1.0,<2.0
python-multipart>=0.0.18,<1.0

# --- PostgreSQL (psycopg v3 — works with Python 3.14) ---
psycopg[binary]>=3.2,<4.0

# --- MongoDB ---
pymongo>=4.12,<5.0

# --- Neo4j & Graph Analytics ---
neo4j>=5.28,<6.0
networkx>=3.0,<4.0

# --- NLP ---
spacy>=3.8,<4.0
rapidfuzz>=3.13,<4.0

# --- LLM (Groq API) ---
groq>=0.25,<1.0

# --- Data Generation & Processing ---
pandas>=2.2,<3.0
openpyxl>=3.1,<4.0
PyMuPDF>=1.25,<2.0
pytesseract>=0.3,<1.0

# --- Auth & Security ---
python-jose[cryptography]>=3.4,<4.0
bcrypt>=4.2,<5.0
cryptography>=44.0,<45.0

# --- Testing ---
pytest>=8.3,<9.0
httpx>=0.28,<1.0

# --- Code Quality ---
ruff>=0.11,<1.0
